/**
 * Empty stub for web-component JS files imported with absolute paths
 * (e.g. /blocks/accordion/qsr-accordion.js).
 *
 * These files register Custom Elements in a real browser; in Node.js
 * tests we only care that the import does not throw, not about the
 * element definition itself.
 */
export default {};
